// lib/features/advisor/presentation/advisor_profile_page.dart

import 'package:flutter/material.dart';

class AdvisorProfilePage extends StatelessWidget {
  const AdvisorProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Profil conseiller"),
      ),
    );
  }
}

